<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liuqi.tools.codelife.db.dao.UserLogDao">

    <insert id="logToDB">
        insert into user_logs(username, user_ip, operate_time, message)
        values(#{username}, #{userIp}, #{operateTime}, #{message})
    </insert>
</mapper>